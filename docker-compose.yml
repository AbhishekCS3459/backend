services:

  postgres:
    image: postgres:16-alpine
    container_name: find-me-backend-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: find_me
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 2s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: find-me-backend-redis
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 2s
      timeout: 5s
      retries: 10

  # api:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: find-me-backend-api
  #   environment:
  #     DATABASE_URL: postgresql://postgres:postgres@postgres:5432/find_me?sslmode=disable
  #     QUEUE_URL: redis://redis:6379
  #     JWT_SECRET: dev-secret-change-in-production
  #     PORT: 8080
  #     ENVIRONMENT: development
  #     LOG_LEVEL: info
  #   ports:
  #     - "8080:8080"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy
  #   command: sh -c "sleep 2 && ./api"

volumes:
  postgres_data:
  redis_data:
